<form method="post" action="{{ path('assunto_delete', {'idassunto': tbassunto.idassunto}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbassunto.idassunto) }}">
    <button class="btn">Deletar</button>
</form>
